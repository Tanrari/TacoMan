
server:
  port : 8090

spring:
  thymeleaf:
    cache : false
  datasource:
    generate-unique-name : false
    name : tacocloud
  jpa:
    defer-datasource-initialization: false

    hibernate:
      ddl-auto: create-drop
taco:
  orders:
    pageSize: 10

